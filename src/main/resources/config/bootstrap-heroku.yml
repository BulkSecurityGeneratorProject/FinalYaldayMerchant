# ===================================================================
# Spring Cloud Config bootstrap configuration for the "heroku" profile
# ===================================================================

spring:
    cloud:
        config:
            fail-fast: true
            uri: https://${JHIPSTER_REGISTRY_USER}:${JHIPSTER_REGISTRY_PASSWORD}@${JHIPSTER_REGISTRY_HOST}/config
